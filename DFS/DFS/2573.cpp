#include <iostream>
#include <cstring>
#include <vector>


using namespace std;

int N, M;
int map[301][301];
bool visited[301][301];

int main() {

	memset(map, 0, sizeof(map));
	memset(visited, false, sizeof(visited));

	cin >> N >> M;

	return 0;
}