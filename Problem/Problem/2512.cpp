#include <iostream>
#include <vector>

using namespace std;

int N, maxBound;
vector<int> cost;

int main() {

	cin >> N;
	for (int i = 0; i < N; i++) {
		int input;
		cin >> input;
		cost.push_back(input);
	}
	cin >> maxBound;





	return 0;
}